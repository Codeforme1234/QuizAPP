/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "media.licdn.com",
      "i.gifer.com",
      "media1.tenor.com",
      "static.wikia.nocookie.net",
    ],
  },
};

export default nextConfig;
